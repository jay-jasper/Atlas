[package]
name = "atlas-ui-schema"
version = "0.1.0"
edition = "2021"
description = "Cross-platform declarative UI schema shared by Atlas plugin runtimes and renderers."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
