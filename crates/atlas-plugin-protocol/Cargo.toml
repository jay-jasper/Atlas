[package]
name = "atlas-plugin-protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
atlas-ui-schema = { path = "../atlas-ui-schema" }
serde = { version = "1", features = ["derive"] }
serde_cbor = "0.11"
thiserror = "2"
